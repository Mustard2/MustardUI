[tool.ruff]
target-version = "py312"
line-length = 100
extend-exclude = ["./armature/external/mhx"]

[tool.ruff.lint]
# E/F are defaults; "I" adds import sorting (isort)
select = ["E", "F", "I"]
# extend-select = ["ARG"]
